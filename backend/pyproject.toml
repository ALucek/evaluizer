[project]
name = "evaluizer"
version = "0.1.0"
description = "Lightweight interface for evaluating and optimizing LLM prompts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.121.1",
    "uvicorn>=0.38.0",
]
